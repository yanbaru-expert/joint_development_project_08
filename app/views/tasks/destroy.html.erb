<%= link_to "投稿画面へ", new_task_path %>
<% @tasks.each do |task| %>
<p>
    <%= task.title %>
    <%= task.content%>
    <%= link_to "削除", task_path(task), method: :delete, data: {confirm: "削除しますか？"} %>
</p>
<% end %>